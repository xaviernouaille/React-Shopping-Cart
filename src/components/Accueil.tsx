import { FunctionComponent as FC } from "react"
import Banner from "../components/Banner"

const Accueil: FC = (): JSX.Element => {
  return (
    <section>
      <Banner title="Accueil" />
    </section>
  )
}

export default Accueil
